$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json

command: >
  python training/train_regression.py
  --data_path ${{inputs.data_path}}
  --feature_version ${{inputs.feature_version}}

code: ../

inputs:
  data_path:
    type: uri_file
    path: azureml:com774-dataset:1
  feature_version:
    type: string
    # Example values: "base", "minmax", "zscore"
    default: base

environment: azureml:cw2-training-env@latest

compute: azureml:cw2-compute

experiment_name: cw2-regression
description: "Regression model training for numeric time_to_resolve with feature versioning"
